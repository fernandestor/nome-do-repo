{
  "name": "projeto-dev-workflow",
  "private": true,
  "version": "1.0.0",
  "description": "Development workflow tooling (commitlint + husky)",
  "scripts": {
    "prepare": "husky",
    "commitlint": "commitlint --edit"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.0",
    "@commitlint/config-conventional": "^19.8.0",
    "husky": "^9.1.7"
  }
}
